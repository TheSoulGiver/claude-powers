# CMX Outputs

Files generated by `scripts/build-matrix.sh`:

- `capability-manifest.latest.md`: full capability scan snapshot
- `capability-matrix.latest.md`: capability-to-task matrix for the input task

Run:

```bash
bash scripts/build-matrix.sh <project-root> "<task text>"
```
